# Copy this content to Streamlit Cloud's Secrets settings
# (App settings > Secrets), or copy to .streamlit/secrets.toml for local dev.
#
# NEVER commit the real secrets.toml to git!

OPENAI_API_KEY = ""
GEMINI_API_KEY = ""

# Optional overrides
# OPENAI_MODEL = "gpt-4o-mini"
# GEMINI_MODEL = "gemini-2.5-flash-lite"
